//divisão

let divisao = parseInt(prompt('Digite o valor do Numerador: '));

divisao /= parseInt(prompt('Digite o valor do Denominador: '));

alert('Resultado da divisão é: '+divisao)

confirm('Resultado estava correto ?')
